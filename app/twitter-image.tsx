/**
 * Twitter image re-exports the OG image so the social card stays in sync
 * with one source of truth. Same 1200x630 light-theme composition.
 */
export {
  default,
  alt,
  size,
  contentType,
} from "./opengraph-image";
